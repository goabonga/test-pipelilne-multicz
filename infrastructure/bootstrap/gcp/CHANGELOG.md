# Changelog — bootstrap/gcp

The GCP state backend bootstrap. Applied by hand, once, with local state —
it creates the bucket every other unit then uses as its backend. See
[`../README.md`](../README.md).
