# Version pin only — everything else comes from ../base/migrations.yaml.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: migrations
  namespace: shomer
spec:
  chart:
    spec:
      version: "0.0.0"
